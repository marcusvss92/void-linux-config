# .bashrc

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# Wayland environment variables for Intel usage
source $HOME/.profile-sway

# Aliases
alias ls='ls --color=auto'

# Shell config
PS1='[\u@\h \W]\$ '
